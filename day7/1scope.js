// let  a=10
// const b=20
// var c=30
// console.log(a);
// console.log(b);


if(true)
{
    let a=100
    const b=200
    var c=300
    console.log(a);
 
}
  // console.log(a);
    //console.log(b);
    console.log(c);

    //block scope - variables declared inside a block (between {}) are not accessible outside the block, but var is an exception to this rule, it is function scoped, so it is accessible outside the block as well.
    //global scope - variables declared outside any function or block are accessible anywhere in the code, but var is an exception to this rule, it is function scoped, so it is not accessible outside the function as well.
    //scope is different in node and browser environment
    