(function chai() {
    console.log('DB connected') ;
})() // this is an IIFE (Immediately Invoked Function Expression), it is a function that is defined and immediately invoked, it is used to create a new scope and avoid polluting the global scope, it is also used to execute some code immediately without having to call the function separately.
